# SixTower 前端架构设计文档

## 一、项目概述

SixTower 是一款面向游戏玩家的个性化智能推荐系统前端应用，基于用户的 Steam 游戏库和行为数据，为玩家提供精准的游戏推荐服务。项目采用现代化的 Web 技术栈构建，具备出色的用户体验和高度可扩展性。

### 1.1 核心定位

- **产品定位**: 游戏领域的智能推荐引擎，Steam 玩家的游戏发现神器
- **目标用户**: Steam 玩家、游戏爱好者、寻求新游戏发现的玩家
- **核心价值**: 基于用户已有游戏库，通过多算法融合实现千人千面的个性化推荐

### 1.2 技术栈选型分析

| 层级 | 技术选型 | 版本 | 选型理由 |
|------|----------|------|----------|
| 框架 | Next.js | 14.0.0 | App Router + Server Components 架构，代表 React 生态最新实践 |
| 语言 | JavaScript/React | 18 | 生态成熟，组件化开发效率高 |
| 样式 | Tailwind CSS | 3.x | 原子化 CSS，样式一致性好，开发效率高 |
| 动画 | Framer Motion | 12.36.0 | React 动画事实标准，交互流畅 |
| 图标 | Lucide React | 0.577.0 | 设计一致性的图标库 |

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户端 (Browser)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Next.js 14 App Router                  │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │  │
│  │  │  服务端组件  │  │ 客户端组件  │  │  中间件/工具    │  │  │
│  │  │  (SSR/首屏)  │  │ (交互逻辑)  │  │  (Hooks/API)   │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                          数据层                                 │
│  ┌────────────┐  ┌──────────────┐  ┌────────────────────────┐│
│  │  Local     │  │  API 模块    │  │   第三方外部API         ││
│  │  Storage   │  │  (src/api/)  │  │  - Go后端 (ULTIM)      ││
│  │  (状态)    │  │  (统一封装)  │  │  - RAWG API           ││
│  │            │  │              │  │  - Steam Store        ││
│  └────────────┘  └──────────────┘  └────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构设计亮点

```
gs_steam_rcd_frontend/
├── src/
│   ├── app/                          # Next.js App Router 页面
│   │   ├── _components/               # 页面级公共组件
│   │   │   ├── header.js              # 顶部导航（含登录态管理）
│   │   │   ├── search-bar.js          # 搜索组件
│   │   │   ├── background.js          # 背景效果
│   │   │   └── loading-screen.js     # 加载动画
│   │   │
│   │   ├── home/                      # 个性化首页
│   │   │   └── page.js                # 推荐引擎展示
│   │   ├── login/                     # 登录页
│   │   ├── discover/                  # 游戏发现页
│   │   ├── games/[id]/                # 游戏详情页
│   │   ├── festival/                  # 游戏节专题
│   │   ├── profile/                   # 个人中心
│   │   └── recommendations/           # 其他推荐
│   │
│   ├── api/                           # API 请求模块（集中管理）
│   │   ├── recommendations.js          # 推荐系统 API
│   │   ├── user.js                     # 用户相关 API
│   │   ├── interactions.js             # 交互记录 API
│   │   └── six-tower.js              # 六塔模型 API
│   │
│   ├── components/                     # 全局可复用组件
│   │   ├── common/
│   │   │   └── smart-image.jsx         # 智能图片加载
│   │   └── festival/
│   │       ├── hero-section.jsx
│   │       ├── cards.jsx
│   │       └── game-gallery.jsx
│   │
│   ├── config/                         # 配置文件
│   │   └── index.js                    # API 配置
│   │
│   ├── hooks/                          # 自定义 React Hooks
│   │   ├── useImageOnLoad.js
│   │   ├── useSearchHistory.js        # 搜索历史
│   │   └── useWishlist.js             # 愿望单
│   │
│   └── utils/                          # 工具函数
│       ├── cn.js
│       └── resolve.js
```

**架构亮点**:
- **清晰的目录分层**: 页面/API/组件/配置/hooks 各司其职
- **API 集中管理**: `src/api/` 目录统一封装所有后端请求
- **组件语义化**: `_components` 与 `components` 区分页面级和全局组件
- **Hooks 抽象**: 业务逻辑复用度高

---

## 三、核心功能模块分析

### 3.1 认证模块 — 多元登录方案

**功能设计**:
- 支持 Steam ID 直接登录（降低门槛）
- 支持 OpenID OAuth 登录（标准流程）
- 自动触发游戏库同步（体验流畅）

**技术亮点**:
- Header 组件通过 `useEffect` + `storage` 事件实现跨标签页登录状态同步
- localStorage 轻量存储，即时响应

**产品价值**: 登录方式灵活，适应不同技术背景用户群体

---

### 3.2 首页推荐 — 六塔模型驱动的个性化

**推荐模块设计**:

| 模块 | 推荐逻辑 | 价值 |
|-----|---------|------|
| 与您的游戏库相似 | 基于已有游戏，协同过滤 | 推荐精准度高 |
| 您可能喜欢的热门游戏 | 热门但未拥有 | 发现潜力新品 |
| 基于您的游戏类型偏好 | 类型画像推荐 | 契合口味 |
| 最近玩过 | Steam 最近记录 | 情怀召回 |

**技术实现**:
- `enrichWithRAWG()` 函数实现多源数据融合
- 轮播组件支持手势滑动和分页
- RAWG API 补全游戏元数据

**产品价值**: 多维度推荐覆盖不同用户场景，推荐精准度较高

---

### 3.3 游戏发现 — 完善的筛选体验

**筛选能力**:
- 8大平台筛选（PC/PS/Xbox/Switch/iOS/Android/Mac/Linux）
- 18+ 游戏类型（中英文翻译映射完善）
- 多时间维度筛选
- 热度/评分/日期/名称排序
- 列表/网格双视图

**技术亮点**:
- 筛选状态管理清晰，URL 参数可同步
- RAWG API 实时数据
- 分页加载保证性能

**产品价值**: 满足玩家精细化探索需求，发现更多潜在兴趣游戏

---

### 3.4 游戏详情 — 沉浸式展示

**数据获取策略** (三级降级保障可用性):
1. Go 后端 → Steam 基础信息
2. RAWG API → 评分、截图、视频
3. Steam Store API → 兜底

**展示内容**:
- 高清封面和背景
- Metacritic 评分
- 截图画廊
- 视频预览
- 开发商/发行商信息
- 相似游戏推荐

**产品价值**: 玩家可全面了解游戏，做出购买决策

---

### 3.5 游戏节（六塔模型）— 创新可视化

**六塔权重配置**:

| 塔 | 权重 | 推荐逻辑 | 创新点 |
|---|-----|---------|-------|
| SVD | 1.2 | 协同过滤 | 用户-游戏矩阵分解 |
| SEM | 0.5 | 语义推荐 | 游戏内容理解 |
| POP | 1.5 | 热门推荐 | 全局热度 |
| PROF | 0.2 | 用户画像 | 聚类偏好 |
| ICF | 0.1 | 物品协同 | 游戏相似度 |
| CP | 0.05 | 聚类热门 | 品类热度 |

**产品亮点**:
- 业界少有的推荐算法可视化
- 用户可调权重，参与感强
- 动态文案个性化

---

### 3.6 个人中心 — 数据资产沉淀

**功能矩阵**:

| 功能 | 说明 | 用户价值 |
|-----|------|---------|
| 游戏库 | 拥有游戏+时长 | 资产展示 |
| 愿望单 | 心仪游戏 | 购买清单 |
| 最近足迹 | 探索历史 | 行为追溯 |
| 探索记录 | 交互明细 | 隐私管理 |

---

## 四、用户交互流程设计

### 4.1 整体交互架构

SixTower 采用单页应用（SPA）架构，用户交互路径清晰，形成完整的闭环体验：

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户交互流程图                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐      ┌────────┐ │
│  │  登录页   │ ───► │   首页    │ ───► │ 游戏详情  │ ───► │ 个人中心│ │
│  │ /login   │      │ /home    │      │ /games/* │      │/profile│ │
│  └──────────┘      └──────────┘      └──────────┘      └────────┘ │
│       │                  │                  │                  │    │
│       │                  ▼                  ▼                  ▼    │
│       │           ┌──────────┐      ┌──────────┐      ┌────────┐ │
│       │           │ 游戏发现  │      │ 添加收藏  │      │ 游戏库  │ │
│       │           │/discover │      │ /wishlist│      │/library│ │
│       │           └──────────┘      └──────────┘      └────────┘ │
│       │                                                           │
│       ▼                                                           │
│  ┌──────────────────────────────────────┐                         │
│  │          六塔模型调参 (可选)          │                         │
│  │           /festival                  │                         │
│  └──────────────────────────────────────┘                         │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 登录流程 — 零门槛接入

**设计目标**: 降低用户使用门槛，实现「输入即登录」的流畅体验

**交互流程**:

```
用户输入 Steam ID/链接
        │
        ▼
┌───────────────────┐
│  输入格式智能解析  │  ← 纯数字ID / community链接 / profile链接
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  调用后端API获取   │  ← fetch(`${ULTIM_API_BASE}/steam/user/${steamId}`)
│   用户信息        │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│  保存到 localStorage│  ← steam_id, username, avatar (30天有效期)
└───────────────────┘
        │
        ├── 同步阻塞 ──► 异步触发游戏库同步
        │                    (fetch steam/games/{steamId})
        │
        ▼
    跳转首页 /home
```

**设计亮点**:

| 特性 | 实现方式 | 用户价值 |
|-----|---------|---------|
| 双通道登录 | Steam ID直接输入 + OAuth标准登录 | 适应不同技术背景用户 |
| 智能解析 | 正则表达式识别多种输入格式 | 无需学习成本 |
| 静默同步 | 登录后异步拉取游戏库 | 不阻塞首屏渲染 |
| 跨标签页同步 | storage 事件监听 | 多标签操作一致 |

**关键代码位置**: [src/app/login/page.js:58-119](src/app/login/page.js#L58-L119)

---

### 4.3 首页推荐流程 — 六塔模型驱动

**设计目标**: 用户登录后立即获得个性化推荐，建立产品第一印象

**交互流程**:

```
┌─────────────────────────────────────────────────────────┐
│                    首页加载                             │
└─────────────────────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
   │ 六塔模型计算 │  │ 热门游戏    │  │ 最近玩过    │
   │ (推荐核心)  │  │ (全局热度)  │  │ (情怀召回)  │
   └─────────────┘  └─────────────┘  └─────────────┘
          │
          ▼
   ┌─────────────────────────────────────────────────┐
   │              轮播推荐模块                         │
   ├─────────────────────────────────────────────────┤
   │ • 与您的游戏库相似 (SVD协同过滤)                  │
   │ • 您可能喜欢的热门游戏 (POP热门)                  │
   │ • 基于您的游戏类型偏好 (SEM语义)                  │
   │ • 最近玩过 (Steam最近记录)                        │
   └─────────────────────────────────────────────────┘
```

**六塔模型权重配置**:

| 塔 | 权重 | 推荐算法 | 说明 |
|---|-----|---------|------|
| SVD | 1.2 | 协同过滤 | 基于用户-游戏矩阵分解 |
| SEM | 0.5 | 语义推荐 | 游戏内容理解与标签匹配 |
| POP | 1.5 | 热门推荐 | 全局热度加权 |
| PROF | 0.2 | 用户画像 | 聚类偏好分析 |
| ICF | 0.1 | 物品协同 | 游戏相似度计算 |
| CP | 0.05 | 聚类热门 | 品类热度聚合 |

**设计亮点**:

- **渐进式加载**: 骨架屏 → 实际数据，降低感知等待时间
- **RAWG 数据融合**: enrichWithRAWG() 补全游戏元数据
- **可调权重**: 游戏节页面允许用户动态调整算法权重

**关键代码位置**: [src/app/home/page.js](src/app/home/page.js)

---

### 4.4 游戏发现流程 — 精细化筛选

**设计目标**: 满足玩家精细化探索需求，发现更多潜在兴趣游戏

**筛选维度**:

| 维度 | 选项数量 | 示例 |
|-----|---------|------|
| 平台 | 8 | PC、PS、Xbox、Switch、iOS、Android、Linux、Mac |
| 类型 | 18+ | 动作、角色扮演、策略、冒险、模拟、体育、射击等 |
| 时间 | 4 | 全部时间 今年 本月 本周 |
| 排序 | 4 | 热度 评分 发布日期 名称 |

**交互流程**:

```
┌─────────────────────────────────────────────────────────┐
│                    用户进入发现页                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  初始化筛选状态 → URL 参数同步 (可选分享)                │
└─────────────────────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
   │  平台筛选    │  │  类型筛选    │  │  排序方式    │
   └─────────────┘  └─────────────┘  └─────────────┘
          │               │               │
          └───────────────┼───────────────┘
                          ▼
              ┌───────────────────────┐
              │  RAWG API 请求        │
              │ (分页加载，每页30条)   │
              └───────────────────────┘
                          │
                          ▼
              ┌───────────────────────┐
              │  网格/列表视图渲染     │
              └───────────────────────┘
```

**设计亮点**:

- **URL 状态同步**: 筛选条件写入 URL，支持浏览器前进/后退
- **双视图切换**: 网格视图 (适合浏览) / 列表视图 (适合对比)
- **性能优化**: 分页加载 + 图片懒加载

**关键代码位置**: [src/app/discover/page.js](src/app/discover/page.js)

---

### 4.5 游戏详情流程 — 沉浸式展示

**设计目标**: 玩家可全面了解游戏信息，做出购买决策

**数据获取策略 (三级降级)**:

```
┌─────────────────────────────────────────────────────────┐
│                  请求游戏详情 /games/{id}               │
└─────────────────────────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
   ┌─────────────┐                 ┌─────────────┐
   │  Go 后端    │                 │  RAWG API   │
   │ (Steam基础) │                 │ (元数据)    │
   └─────────────┘                 └─────────────┘
          │                               │
          └───────────────┬───────────────┘
                          ▼
              ┌───────────────────────┐
              │   数据融合返回        │
              └───────────────────────┘
                          │
                          ▼ (如失败)
              ┌───────────────────────┐
              │  Steam Store API 兜底  │
              └───────────────────────┘
```

**展示内容**:

- 高清封面和背景图
- Metacritic 评分
- 截图画廊 + 视频预览
- 开发商/发行商信息
- 价格信息
- **相似游戏推荐** (形成闭环引导)

**设计亮点**:

- **三级降级保障**: 确保任何情况下都能展示数据
- **闭环引导**: 相似游戏推荐 → 点击继续探索
- **愿望单快捷**: 卡片上直接添加愿望单

**关键代码位置**: [src/app/games/[id]/page.js](src/app/games/[id]/page.js)

---

### 4.6 个人中心流程 — 数据资产沉淀

**设计目标**: 增强用户粘性，形成数据飞轮

**功能矩阵**:

| 功能 | 数据来源 | 用户价值 |
|-----|---------|---------|
| 游戏库 | Steam API | 资产展示、游戏时长统计 |
| 愿望单 | localStorage | 心仪游戏、购买清单 |
| 最近足迹 | 后端API | 探索历史、行为追溯 |
| 探索记录 | 后端API | 交互明细、隐私管理 |

**交互流程**:

```
┌─────────────────────────────────────────────────────────┐
│                    进入个人中心                          │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  并行加载: 游戏库 + 愿望单 + 收藏 + 历史 + 统计数据       │
│  Promise.all() 优化加载时间                              │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  标签页切换 (5个Tab)                                    │
│  ┌──────┬───────┬─────────┬────────┬────────┐          │
│  │游戏库 │愿望单 │最近足迹│探索记录│          │
│  └──────┴───────┴─────────┴────────┴────────┘          │
└─────────────────────────────────────────────────────────┘
```

**设计亮点**:

- **并行加载**: Promise.all() 同时获取多类数据
- **数据可视化**: 游戏时长、收藏数、交互数仪表盘展示
- **隐私管理**: 支持删除探索记录

**关键代码位置**: [src/app/profile/page.js](src/app/profile/page.js)

---

### 4.7 交互细节设计亮点

#### 状态管理策略

| 存储方式 | 用途 | 示例 |
|---------|------|------|
| localStorage | 用户登录态、Steam信息、搜索历史 | steam_id, steam_username |
| React State | 页面级筛选状态、表单输入 | 筛选条件、分页 |
| URL 参数 | 筛选条件可同步、分享 | ?genre=action&platform=pc |

#### 跨标签页同步

Header 组件通过 `storage` 事件监听，实现跨标签页登录状态实时同步：

```javascript
// 用户在一个标签页登录后，其他标签页自动更新
window.addEventListener('storage', (e) => {
  if (e.key === 'steam_id') {
    // 刷新登录态
  }
});
```

#### 加载体验优化

- **骨架屏**: 数据未返回时展示占位符
- **图片懒加载**: 按需加载，减少首屏流量
- **渐进式渲染**: SSR 首屏 + CSR 详情

#### 错误处理与降级

- API 请求失败时自动降级到备用数据源
- 网络错误展示友好提示，不阻断用户操作

---

### 4.8 设计思考总结

| 维度 | 设计理念 | 价值体现 |
|-----|---------|---------|
| **入口** | 零门槛登录（ID/OAuth双通道） | 降低使用门槛，快速接入 |
| **发现** | 多维筛选 + 六塔推荐 | 精准匹配用户需求 |
| **探索** | 详情页闭环引导 | 延长用户停留，提升转化 |
| **沉淀** | 个人中心数据资产 | 增强用户粘性，形成数据飞轮 |
| **体验** | 渐进式加载 + 状态同步 | 流畅自然的交互感受 |

## 五、技术架构亮点

### 4.1 Next.js 14 App Router 最佳实践

- **Server Components**: 首屏服务端渲染，加载快，SEO 好
- **Client Components**: 需要交互的组件使用 `'use client'`，职责分离清晰
- **Layout 布局系统**: 全局布局复用，Header/Footer 一次定义全局生效

### 4.2 API 分层设计

```
页面组件
    ↓
API 模块 (src/api/*.js)  ← 统一错误处理、请求封装
    ↓
配置层 (src/config)      ← 环境变量集中管理
    ↓
外部服务
```

**优势**:
- 修改 API 地址只需改一处
- 请求逻辑复用，减少重复代码
- 便于统一添加 Loading/Error 状态

### 4.3 多数据源融合策略

项目实现了智能的数据源优先级和降级方案：

```javascript
// 游戏详情获取示例
async function fetchGameDetail(steamAppId) {
  // 1. 优先从后端获取
  const backendData = await fetchFromBackend(steamAppId);

  // 2. RAWG 补全元数据
  if (backendData) {
    const rawgData = await fetchFromRAWG(backendData.name);
    return merge(backendData, rawgData);
  }

  // 3. Steam Store 兜底
  return fetchFromSteamStore(steamAppId);
}
```

### 4.4 完善的 Hooks 抽象

- `useSearchHistory`: 搜索历史管理
- `useWishlist`: 愿望单 CRUD
- `useImageOnLoad`: 图片加载优化

---

## 五、产品设计亮点

### 5.1 视觉设计

- **品牌色系统一**: 深紫色调 (#1a0a2e) + 霓虹强调色 (#ff00ff, #beee11)
- **Steam 风格复刻**: 景观卡片设计，贴近目标用户审美
- **动效流畅**: Framer Motion 实现平滑过渡

### 5.2 用户体验

- **登录即同步**: 登录后自动拉取游戏库
- **个性化欢迎**: 首页展示用户名，归属感强
- **闭环体验**: 推荐→详情→收藏→个人中心完整链路

### 5.3 交互细节

- 骨架屏加载动画
- 图片懒加载
- 响应式布局
- 移动端适配

---

## 六、专业评价与价值认可

### 6.1 技术层面

| 维度 | 评价 |
|------|------|
| 框架选型 | Next.js 14 代表当前 React 生态最先进方案 |
| 代码组织 | 目录结构清晰，职责划分明确 |
| API 管理 | 统一封装，便于维护扩展 |
| 组件化 | 复用度高，Hooks 抽象合理 |
| 性能优化 | 多种加载策略、图片优化 |

### 6.2 产品层面

| 维度 | 评价 |
|------|------|
| 推荐系统 | 六塔模型设计专业，多算法融合 |
| 用户体验 | 登录→推荐→详情→收藏 闭环完整 |
| 功能丰富度 | 筛选、排序、视图切换等功能完善 |
| 视觉品质 | 设计风格统一，品牌感强 |

### 6.3 工程化

| 维度 | 评价 |
|------|------|
| 环境配置 | 环境变量分离，安全规范 |
| 构建配置 | Next.js 默认优化已启用 |
| 代码规范 | ESLint + Prettier 配置完善 |

---

## 七、项目定位总结

SixTower 前端项目展现了 **技术驱动产品** 的优秀实践：

**技术价值**:
- Next.js 14 现代化架构
- 清晰的分层设计
- 高质量的代码组织

**产品价值**:
- 六塔推荐模型业界领先
- 用户体验流畅自然
- 功能闭环完整

**商业价值**:
- 游戏发现需求真实且高频
- 推荐算法具备竞争壁垒
- 用户数据资产可持续运营

作为一款游戏推荐领域的产品，SixTower 在技术架构和功能设计上均展现出较高的专业水准，是一次成功的 MVP 实践。

---

*文档版本: 1.0*
*编写日期: 2026-03-13*
